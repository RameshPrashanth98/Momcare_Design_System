---
phase: 01-foundation-infrastructure
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tokens/colors.ts
  - tokens/typography.ts
  - tokens/spacing.ts
  - tokens/radius.ts
  - tokens/shadows.ts
  - tokens/grid.ts
  - tokens/index.ts
  - tailwind.config.ts
autonomous: true
requirements:
  - FOUN-03
  - FOUN-04
  - FOUN-05
  - FOUN-06
  - FOUN-07
  - FOUN-08

must_haves:
  truths:
    - "Every color token (Rose, Cream, Sage, Neutral, Status, Semantic) resolves to the exact hex value in morph-maternity-design-system.html"
    - "Font families CormorantGaramond-Regular, DMSans-Regular, DMMono-Regular are defined in tailwind.config.ts fontFamily — keys match useFonts keys exactly"
    - "spacing-1 through spacing-48 are all defined (4px grid, 15 steps)"
    - "Radius tokens none through full are all defined"
    - "Shadow tokens xs through 2xl plus inner are all defined"
    - "Grid breakpoints sm (640px), md (768px), lg (1024px) are defined"
    - "Token files are TypeScript const exports — no framework deps, importable in tailwind.config.ts"
  artifacts:
    - path: "tokens/colors.ts"
      provides: "Rose, Cream, Sage, Neutral palettes + Status + Semantic color constants"
      exports: ["colors"]
    - path: "tokens/typography.ts"
      provides: "Font family keys, type scale, font weights, line heights"
      exports: ["fontFamily", "fontSize", "fontWeight", "lineHeight"]
    - path: "tokens/spacing.ts"
      provides: "4px grid spacing scale space-1 through space-48"
      exports: ["spacing"]
    - path: "tokens/radius.ts"
      provides: "Border radius scale none through full"
      exports: ["radius"]
    - path: "tokens/shadows.ts"
      provides: "Shadow/elevation tokens xs through 2xl + inner (iOS + Android properties)"
      exports: ["shadows"]
    - path: "tokens/grid.ts"
      provides: "Breakpoint definitions for mobile/tablet/desktop"
      exports: ["breakpoints"]
    - path: "tailwind.config.ts"
      provides: "All tokens bridged into Tailwind theme.extend"
      contains: "colors.rose"
  key_links:
    - from: "tokens/typography.ts"
      to: "app/_layout.tsx useFonts()"
      via: "fontFamily key strings must match exactly"
      pattern: "CormorantGaramond-Regular"
    - from: "tokens/colors.ts"
      to: "tailwind.config.ts theme.extend.colors"
      via: "direct import and spread"
      pattern: "import.*colors.*from.*tokens/colors"
    - from: "tailwind.config.ts"
      to: "NativeWind className resolution"
      via: "presets: [require('nativewind/preset')] + theme.extend"
      pattern: "nativewind/preset"
---

<objective>
Define all Morph Maternity design tokens in TypeScript constant files under tokens/ and bridge them into tailwind.config.ts so every token becomes a usable className.

Purpose: All 50 v1 components will reference token-derived class names exclusively. This plan is the single source of truth for all design values. Token values must exactly match morph-maternity-design-system.html — no approximations.

Output: Six token files + tokens/index.ts + updated tailwind.config.ts with all theme.extend entries populated.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-VALIDATION.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Token pipeline from RESEARCH.md: tokens/*.ts → tailwind.config.ts → NativeWind class names → components -->
<!-- Font key strings in tokens/typography.ts MUST match these exact strings used in app/_layout.tsx useFonts(): -->
<!-- "CormorantGaramond-Regular", "CormorantGaramond-Italic", "CormorantGaramond-SemiBold", "CormorantGaramond-Bold" -->
<!-- "DMSans-Regular", "DMSans-Medium" -->
<!-- "DMMono-Regular" -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Color, typography, and spacing token files</name>
  <files>
    tokens/colors.ts,
    tokens/typography.ts,
    tokens/spacing.ts,
    tokens/index.ts
  </files>
  <behavior>
    - colors.rose[50] is defined and is a valid hex string
    - colors.cream[50] is defined
    - colors.sage[50] is defined
    - colors.neutral[900] is defined
    - colors.status.success, warning, error, info are all defined
    - spacing["1"] === 4
    - spacing["4"] === 16
    - spacing["48"] is defined
  </behavior>
  <action>
    Read `morph-maternity-design-system.html` (in the project root or referenced location) to extract exact token values. The HTML file contains CSS custom properties in a `:root` block — copy values from there directly.

    **STEP 1: Locate and read the HTML spec file.**
    Check the project root and any `docs/` or `design/` directories for `morph-maternity-design-system.html`. Read it to extract all CSS custom property values.

    **tokens/colors.ts:**
    Extract all color values from the HTML spec `:root` CSS variables. Structure as follows (fill in exact hex values from the spec):

    ```typescript
    export const colors = {
      // Rose palette — primary brand color
      rose: {
        50:  "#FFF0F3",   // --color-rose-50 from spec
        100: "#FFE0E8",
        200: "#FFC0D1",
        300: "#FF91AA",
        400: "#FF6B89",
        500: "#FF2D55",   // primary brand rose
        600: "#E0184F",
        700: "#BE0F44",
        800: "#9C1040",
        900: "#82133B",
        950: "#47051C",
      },
      // Cream palette — warm neutral background
      cream: {
        50:  "#FFFDF8",
        100: "#FFF8EC",
        200: "#FFF0D6",
        300: "#FFE4B5",
        400: "#FFD485",
        500: "#FFC04A",
        600: "#E0A030",
        700: "#B87D1A",
        800: "#8F5E11",
        900: "#6B440D",
        950: "#3D2605",
      },
      // Sage palette — soft green accent
      sage: {
        50:  "#F4F8F4",
        100: "#E6F0E7",
        200: "#CAE0CD",
        300: "#A4CAA9",
        400: "#74AE7B",
        500: "#4E9157",
        600: "#3B7644",
        700: "#2F5E37",
        800: "#264A2D",
        900: "#1F3D25",
        950: "#0F2214",
      },
      // Neutral palette — grays
      neutral: {
        50:  "#FAFAFA",
        100: "#F5F5F5",
        200: "#E5E5E5",
        300: "#D4D4D4",
        400: "#A3A3A3",
        500: "#737373",
        600: "#525252",
        700: "#404040",
        800: "#262626",
        900: "#171717",
        950: "#0A0A0A",
      },
      // Status colors
      status: {
        success: "#22C55E",
        successLight: "#DCFCE7",
        warning: "#F59E0B",
        warningLight: "#FEF3C7",
        error:   "#EF4444",
        errorLight: "#FEE2E2",
        info:    "#3B82F6",
        infoLight: "#DBEAFE",
      },
    } as const;

    // Semantic color aliases — used for component defaults
    export const semanticColors = {
      primary:    colors.rose[500],
      primaryDark: colors.rose[600],
      surface:    colors.cream[50],
      background: colors.cream[100],
      textPrimary: colors.neutral[900],
      textSecondary: colors.neutral[600],
      textDisabled: colors.neutral[400],
      border:     colors.neutral[200],
      borderFocus: colors.rose[400],
    } as const;
    ```

    IMPORTANT: The placeholder hex values above are estimates. You MUST read morph-maternity-design-system.html and replace every hex value with the exact value from the spec. Do not approximate.

    **tokens/typography.ts:**
    Font keys must match useFonts() keys in app/_layout.tsx EXACTLY (case-sensitive, hyphen vs space matters):

    ```typescript
    // CRITICAL: These key strings must match useFonts() keys in app/_layout.tsx exactly
    export const fontFamily = {
      display: ["CormorantGaramond-Regular"],    // Cormorant Garamond — display/editorial text
      displayItalic: ["CormorantGaramond-Italic"],
      displaySemiBold: ["CormorantGaramond-SemiBold"],
      body: ["DMSans-Regular"],                   // DM Sans — body and UI text
      bodyMedium: ["DMSans-Medium"],
      mono: ["DMMono-Regular"],                   // DM Mono — code/data
    } as const;

    // Type scale — extract exact values from morph-maternity-design-system.html
    export const fontSize = {
      xs:   { size: 10, lineHeight: 14 },
      sm:   { size: 12, lineHeight: 16 },
      base: { size: 14, lineHeight: 20 },
      md:   { size: 16, lineHeight: 24 },
      lg:   { size: 18, lineHeight: 28 },
      xl:   { size: 20, lineHeight: 30 },
      "2xl": { size: 24, lineHeight: 32 },
      "3xl": { size: 30, lineHeight: 38 },
      "4xl": { size: 36, lineHeight: 44 },
      "5xl": { size: 48, lineHeight: 56 },
      "6xl": { size: 60, lineHeight: 68 },
    } as const;

    export const fontWeight = {
      light:   "300",
      regular: "400",
      medium:  "500",
      semibold: "600",
    } as const;
    ```

    IMPORTANT: Read morph-maternity-design-system.html for exact font size and line height values. Replace placeholders above with exact spec values.

    **tokens/spacing.ts** (4px base grid — these values are definitive per FOUN-05):
    ```typescript
    // 4px base grid: space-N = N * 4px
    export const spacing = {
      "1":  4,
      "2":  8,
      "3":  12,
      "4":  16,
      "5":  20,
      "6":  24,
      "8":  32,
      "10": 40,
      "12": 48,
      "16": 64,
      "20": 80,
      "24": 96,
      "32": 128,
      "40": 160,
      "48": 192,
    } as const;
    ```

    **tokens/index.ts** (barrel for token files):
    ```typescript
    export * from "./colors";
    export * from "./typography";
    export * from "./spacing";
    export * from "./radius";
    export * from "./shadows";
    export * from "./grid";
    ```
  </action>
  <verify>
    <automated>npx jest tests/tokens/colors.test.ts tests/tokens/spacing.test.ts --passWithNoTests 2>&1 | tail -15</automated>
  </verify>
  <done>tests/tokens/colors.test.ts passes: all palette keys exist with defined hex values. tests/tokens/spacing.test.ts passes: space-1 through space-48 all defined, space-1===4, space-4===16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Radius, shadow, grid token files + tailwind.config.ts full wiring</name>
  <files>
    tokens/radius.ts,
    tokens/shadows.ts,
    tokens/grid.ts,
    tailwind.config.ts
  </files>
  <behavior>
    - radius.none === 0
    - radius.full is defined
    - shadows.xs has shadowColor property
    - shadows.inner is defined
    - tailwind.config.ts theme.extend.colors includes rose, cream, sage, neutral keys
    - tailwind.config.ts theme.extend.fontFamily includes display, body, mono
    - tailwind.config.ts theme.extend.screens includes sm and md
  </behavior>
  <action>
    **tokens/radius.ts** (read exact values from morph-maternity-design-system.html):
    ```typescript
    export const radius = {
      none:  0,
      xs:    2,
      sm:    4,
      md:    8,
      lg:    12,
      xl:    16,
      "2xl": 20,
      "3xl": 24,
      full:  9999,
    } as const;
    ```

    **tokens/shadows.ts** (React Native shadow requires both iOS and Android properties):
    ```typescript
    // Shadow tokens for React Native (both iOS shadowColor/shadowOffset/shadowOpacity/shadowRadius
    // and Android elevation are provided; NativeWind applies the correct set per platform)
    export const shadows = {
      xs: {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 1 },
        shadowOpacity: 0.04,
        shadowRadius: 2,
        elevation: 1,
      },
      sm: {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.06,
        shadowRadius: 4,
        elevation: 2,
      },
      md: {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.08,
        shadowRadius: 8,
        elevation: 4,
      },
      lg: {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 8 },
        shadowOpacity: 0.10,
        shadowRadius: 16,
        elevation: 8,
      },
      xl: {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 12 },
        shadowOpacity: 0.12,
        shadowRadius: 24,
        elevation: 12,
      },
      "2xl": {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 24 },
        shadowOpacity: 0.16,
        shadowRadius: 48,
        elevation: 24,
      },
      inner: {
        shadowColor: "#000000",
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.06,
        shadowRadius: 4,
        elevation: 0, // inner shadows have no Android elevation equivalent
      },
    } as const;
    ```

    NOTE: NativeWind v4 does not have native boxShadow support for React Native (web-only). Shadow tokens are used as StyleSheet objects or via the `shadow-*` Tailwind utilities that NativeWind maps to React Native shadow props. Read RESEARCH.md pitfall section for platform variant guidance. Values above are estimates — check morph-maternity-design-system.html for exact values.

    **tokens/grid.ts** (breakpoints for NativeWind v4 mobile-first responsive):
    ```typescript
    // Breakpoints: mobile 4-col (default), tablet 8-col (sm), desktop 12-col (md+)
    export const breakpoints = {
      sm: "640px",   // tablet threshold
      md: "768px",   // wide tablet
      lg: "1024px",  // desktop / large tablet landscape
      xl: "1280px",
    } as const;
    ```

    **tailwind.config.ts** (complete — replaces the skeleton from Plan 01, imports all token files):
    ```typescript
    import type { Config } from "tailwindcss";
    import { colors, semanticColors } from "./tokens/colors";
    import { fontFamily, fontSize, fontWeight } from "./tokens/typography";
    import { spacing } from "./tokens/spacing";
    import { radius } from "./tokens/radius";
    import { breakpoints } from "./tokens/grid";

    // Convert fontSize token map to Tailwind format [size, { lineHeight }]
    const tailwindFontSize = Object.fromEntries(
      Object.entries(fontSize).map(([key, val]) => [
        key,
        [val.size, { lineHeight: String(val.lineHeight) }] as [number, { lineHeight: string }],
      ])
    );

    const config: Config = {
      content: [
        "./app/**/*.{js,jsx,ts,tsx}",
        "./src/**/*.{js,jsx,ts,tsx}",
        "./.rnstorybook/**/*.{js,jsx,ts,tsx}",
      ],
      presets: [require("nativewind/preset")],
      theme: {
        extend: {
          colors: {
            rose:    colors.rose,
            cream:   colors.cream,
            sage:    colors.sage,
            neutral: colors.neutral,
            // Status
            success: colors.status.success,
            "success-light": colors.status.successLight,
            warning: colors.status.warning,
            "warning-light": colors.status.warningLight,
            error:   colors.status.error,
            "error-light": colors.status.errorLight,
            info:    colors.status.info,
            "info-light": colors.status.infoLight,
            // Semantic aliases
            primary:   semanticColors.primary,
            "primary-dark": semanticColors.primaryDark,
            surface:   semanticColors.surface,
            background: semanticColors.background,
          },
          fontFamily: {
            display:      fontFamily.display,
            "display-italic": fontFamily.displayItalic,
            "display-semibold": fontFamily.displaySemiBold,
            body:         fontFamily.body,
            "body-medium": fontFamily.bodyMedium,
            mono:         fontFamily.mono,
          },
          fontSize: tailwindFontSize,
          fontWeight: fontWeight,
          spacing: spacing,
          borderRadius: radius,
          screens: breakpoints,
        },
      },
      plugins: [],
    };

    export default config;
    ```

    After writing tailwind.config.ts, verify no TypeScript errors by running:
    ```bash
    npx tsc --noEmit 2>&1 | head -20
    ```
    Fix any type errors before marking task done.

    Also verify the token-to-Tailwind bridge is working by running the CSS generation:
    ```bash
    npx tailwindcss -i global.css -o /tmp/test-output.css --content "src/**/*.tsx" 2>&1 | head -10
    ```
  </action>
  <verify>
    <automated>npx jest tests/tokens/radius.test.ts tests/tokens/shadows.test.ts tests/tokens/grid.test.ts --passWithNoTests 2>&1 | tail -15</automated>
  </verify>
  <done>All three token tests pass. tailwind.config.ts imports from all token files without TypeScript errors. `npx jest` on all token tests shows green for colors, spacing, radius, shadows, grid.</done>
</task>

</tasks>

<verification>
Overall plan checks:
1. `npx jest tests/tokens/` — all 5 token test files pass
2. `npx tsc --noEmit` exits 0 (no type errors in token files or tailwind config)
3. tokens/colors.ts: all four palettes + status + semantic exported
4. tailwind.config.ts: `theme.extend.colors.rose` resolves to colors.rose palette
5. Font key strings in typography.ts fontFamily exactly match strings in app/_layout.tsx useFonts() keys
6. spacing["1"] === 4, spacing["48"] === 192
7. radius.none === 0, radius.full === 9999
8. shadows.xs, shadows.inner both have shadowColor property
</verification>

<success_criteria>
All 13 Morph Maternity token categories (colors ×5 palettes, typography, spacing, radius, shadows, breakpoints) are defined as TypeScript constants and bridged into tailwind.config.ts. Any subsequent component can use `className="bg-rose-500 font-body text-md"` and receive exact Morph Maternity brand values.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md`
</output>
