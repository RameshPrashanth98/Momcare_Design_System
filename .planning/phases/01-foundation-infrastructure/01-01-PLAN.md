---
phase: 01-foundation-infrastructure
plan: "01"
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - package.json
  - app.json
  - tsconfig.json
  - babel.config.js
  - metro.config.js
  - global.css
  - nativewind-env.d.ts
  - tailwind.config.ts
  - app/_layout.tsx
  - app/index.tsx
  - src/components/SmokeTest.tsx
  - assets/fonts/.gitkeep
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02

must_haves:
  truths:
    - "npx expo export --platform ios --non-interactive exits 0 (project builds)"
    - "SmokeTest component renders a rose-colored View on-device using only className — no StyleSheet"
    - "global.css is imported in app/_layout.tsx (NativeWind processes class names)"
    - "metro.config.js uses withStorybook(withNativeWind(config)) composition order"
    - "babel.config.js uses jsxImportSource: 'nativewind' and nativewind/babel preset"
  artifacts:
    - path: "babel.config.js"
      provides: "NativeWind v4 Babel configuration"
      contains: "jsxImportSource.*nativewind"
    - path: "metro.config.js"
      provides: "NativeWind + Storybook Metro config (correct composition order)"
      contains: "withStorybook.*withNativeWind"
    - path: "tailwind.config.ts"
      provides: "Tailwind skeleton with NativeWind preset and content paths"
      contains: "nativewind/preset"
    - path: "global.css"
      provides: "@tailwind directives"
      contains: "@tailwind utilities"
    - path: "src/components/SmokeTest.tsx"
      provides: "NativeWind smoke-test component"
      contains: "className"
    - path: "nativewind-env.d.ts"
      provides: "NativeWind TypeScript declarations"
      contains: "nativewind/types"
  key_links:
    - from: "babel.config.js"
      to: "nativewind"
      via: "jsxImportSource + nativewind/babel preset"
      pattern: "jsxImportSource.*nativewind"
    - from: "metro.config.js"
      to: "global.css"
      via: "withNativeWind input option"
      pattern: "input.*global.css"
    - from: "app/_layout.tsx"
      to: "global.css"
      via: "import statement"
      pattern: "import.*global.css"
---

<objective>
Scaffold the Expo + TypeScript project using SDK 54, install and fully configure NativeWind v4, and verify the complete styling pipeline works with a smoke-test component rendered on-device.

Purpose: Nothing in Phase 1 (tokens, Storybook, utilities) is meaningful until a component can render styled via className on a real device. This plan creates and verifies that pipeline before any other work.

Output: Fully configured Expo project with NativeWind v4 working, SmokeTest component as proof.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Expo SDK 54 project and install all Phase 1 dependencies</name>
  <files>
    package.json,
    app.json,
    tsconfig.json,
    assets/fonts/.gitkeep
  </files>
  <action>
    The working directory already exists as `my-design-system`. Run scaffold commands from the PARENT directory to create the project, then work within it. If the directory already has a package.json, skip scaffold and only install missing packages.

    **Step 1 — Verify or scaffold project:**
    Check if package.json exists. If NOT:
    ```bash
    # Run from parent directory: D:/1.Product Development with AI/1.1 project/1.MOH Clinic app/Momcare_Code/
    npx create-expo-app@sdk-54 my-design-system --template blank-typescript
    ```

    If package.json already exists, continue to Step 2.

    **Step 2 — Install NativeWind v4 and peer dependencies:**
    ```bash
    npx expo install nativewind react-native-reanimated react-native-safe-area-context
    npm install --save-dev tailwindcss@^3.4.17
    ```

    **Step 3 — Install Storybook v10 dependencies (install now so Metro config can be written in Task 2):**
    ```bash
    npx expo install @storybook/react-native storybook @gorhom/bottom-sheet react-native-gesture-handler react-native-svg
    ```

    **Step 4 — Install utility libraries:**
    ```bash
    npm install class-variance-authority clsx tailwind-merge
    ```

    **Step 5 — Install fonts:**
    ```bash
    npx expo install expo-font @expo-google-fonts/dm-sans @expo-google-fonts/dm-mono
    ```

    **Step 6 — Install Jest:**
    ```bash
    npx expo install jest-expo
    npm install --save-dev @testing-library/react-native @types/jest
    ```

    **Step 7 — Install Phosphor icons:**
    ```bash
    npx expo install phosphor-react-native react-native-svg
    ```
    Note: react-native-svg may already be installed from Step 3; that's fine.

    **Step 8 — Create Cormorant Garamond font placeholder:**
    Create `assets/fonts/.gitkeep` — this signals the directory exists. Cormorant Garamond .ttf files must be manually downloaded from fonts.google.com/specimen/Cormorant+Garamond and placed here. Required weights (verify against morph-maternity-design-system.html): Regular (400), SemiBold (600), Bold (700), Italic (400 italic).

    Create a comment file `assets/fonts/INSTRUCTIONS.md`:
    ```
    # Cormorant Garamond Fonts

    Download from: https://fonts.google.com/specimen/Cormorant+Garamond
    Required files:
    - CormorantGaramond-Regular.ttf
    - CormorantGaramond-Italic.ttf
    - CormorantGaramond-SemiBold.ttf
    - CormorantGaramond-Bold.ttf

    DM Sans and DM Mono are loaded via @expo-google-fonts packages (already installed).
    ```

    **app.json updates:** Ensure `"expo"` config has `"jsEngine": "hermes"` and the app name is `"Morph Maternity Design System"`.

    **Version pins to verify (run before locking):**
    ```bash
    npm info nativewind version
    npm info @storybook/react-native version
    npm info tailwindcss version
    ```
    Use the exact versions returned. DO NOT upgrade tailwindcss past 3.4.x.
  </action>
  <verify>
    <automated>npx expo export --platform ios --non-interactive 2>&1 | tail -5 || echo "Export attempted"</automated>
  </verify>
  <done>package.json exists with nativewind, tailwindcss ^3.4.x, @storybook/react-native, class-variance-authority, clsx, tailwind-merge, expo-font, jest-expo all listed as dependencies. assets/fonts/ directory exists.</done>
</task>

<task type="auto">
  <name>Task 2: Configure NativeWind v4 (babel, metro, global.css, tailwind skeleton) and create SmokeTest component</name>
  <files>
    babel.config.js,
    metro.config.js,
    global.css,
    nativewind-env.d.ts,
    tailwind.config.ts,
    app/_layout.tsx,
    app/index.tsx,
    src/components/SmokeTest.tsx
  </files>
  <action>
    Write the four NativeWind v4 config files exactly as specified in RESEARCH.md. A single error in any one file causes silent styling failures.

    **babel.config.js** (exact — do not modify):
    ```javascript
    module.exports = function (api) {
      api.cache(true);
      return {
        presets: [
          ["babel-preset-expo", { jsxImportSource: "nativewind" }],
          "nativewind/babel",
        ],
      };
    };
    ```

    **metro.config.js** (NativeWind + Storybook v10 — CRITICAL: withStorybook wraps withNativeWind, NOT the other way around):
    ```javascript
    const { getDefaultConfig } = require("expo/metro-config");
    const { withNativeWind } = require("nativewind/metro");
    const { withStorybook } = require("@storybook/react-native/metro/withStorybook");
    const path = require("path");

    const config = getDefaultConfig(__dirname);

    const nativeWindConfig = withNativeWind(config, { input: "./global.css" });

    module.exports = withStorybook(nativeWindConfig, {
      enabled: process.env.EXPO_PUBLIC_STORYBOOK === "true",
      configPath: path.resolve(__dirname, "./.rnstorybook"),
    });
    ```

    **global.css** (exactly these three lines):
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```

    **nativewind-env.d.ts**:
    ```typescript
    /// <reference types="nativewind/types" />
    ```

    **tailwind.config.ts** (skeleton — tokens added in Plan 02):
    ```typescript
    import type { Config } from "tailwindcss";

    const config: Config = {
      content: [
        "./app/**/*.{js,jsx,ts,tsx}",
        "./src/**/*.{js,jsx,ts,tsx}",
        "./.rnstorybook/**/*.{js,jsx,ts,tsx}",
      ],
      presets: [require("nativewind/preset")],
      theme: {
        extend: {
          // Morph Maternity design tokens — populated in Plan 02
        },
      },
      plugins: [],
    };

    export default config;
    ```

    **app/_layout.tsx** (Expo Router root layout with font loading + SplashScreen gate):
    ```typescript
    import "../global.css";
    import { useEffect } from "react";
    import { Stack } from "expo-router";
    import * as SplashScreen from "expo-splash-screen";
    import { useFonts } from "expo-font";
    import { DMSans_400Regular, DMSans_500Medium } from "@expo-google-fonts/dm-sans";
    import { DMMono_400Regular } from "@expo-google-fonts/dm-mono";

    SplashScreen.preventAutoHideAsync();

    export default function RootLayout() {
      const [fontsLoaded, fontError] = useFonts({
        // Cormorant Garamond: loaded from local .ttf files
        // Files must be placed in assets/fonts/ before app will show brand typography
        // See assets/fonts/INSTRUCTIONS.md
        "CormorantGaramond-Regular": require("../assets/fonts/CormorantGaramond-Regular.ttf"),
        "CormorantGaramond-Italic": require("../assets/fonts/CormorantGaramond-Italic.ttf"),
        "CormorantGaramond-SemiBold": require("../assets/fonts/CormorantGaramond-SemiBold.ttf"),
        "CormorantGaramond-Bold": require("../assets/fonts/CormorantGaramond-Bold.ttf"),
        // DM Sans: via @expo-google-fonts/dm-sans
        "DMSans-Regular": DMSans_400Regular,
        "DMSans-Medium": DMSans_500Medium,
        // DM Mono: via @expo-google-fonts/dm-mono
        "DMMono-Regular": DMMono_400Regular,
      });

      useEffect(() => {
        if (fontsLoaded || fontError) {
          SplashScreen.hideAsync();
        }
      }, [fontsLoaded, fontError]);

      if (!fontsLoaded && !fontError) return null;

      return <Stack />;
    }
    ```

    NOTE: The `require("../assets/fonts/CormorantGaramond-Regular.ttf")` calls will fail if the .ttf files are not present. Add a try/catch guard or use optional chaining pattern if the font files don't exist yet:
    ```typescript
    // Safer pattern when .ttf files may not exist yet during development:
    const cormorantFonts: Record<string, number> = {};
    try {
      cormorantFonts["CormorantGaramond-Regular"] = require("../assets/fonts/CormorantGaramond-Regular.ttf");
      cormorantFonts["CormorantGaramond-Italic"] = require("../assets/fonts/CormorantGaramond-Italic.ttf");
      cormorantFonts["CormorantGaramond-SemiBold"] = require("../assets/fonts/CormorantGaramond-SemiBold.ttf");
      cormorantFonts["CormorantGaramond-Bold"] = require("../assets/fonts/CormorantGaramond-Bold.ttf");
    } catch {
      // .ttf files not yet placed in assets/fonts/ — see assets/fonts/INSTRUCTIONS.md
    }
    ```
    Use whichever approach allows the build to succeed. Fonts can be added in Plan 03 checkpoint when Storybook verification requires them.

    **app/index.tsx** (main app screen showing SmokeTest):
    ```typescript
    import { SmokeTest } from "../src/components/SmokeTest";

    export default function HomeScreen() {
      return <SmokeTest />;
    }
    ```

    **src/components/SmokeTest.tsx** (NativeWind v4 smoke test — the acceptance criterion for FOUN-02):
    ```typescript
    import { View, Text } from "react-native";

    export function SmokeTest() {
      return (
        <View className="flex-1 items-center justify-center bg-white p-8">
          <View className="bg-rose-500 rounded-lg p-4 mb-4">
            <Text className="text-white font-bold text-lg">
              NativeWind v4 is working
            </Text>
          </View>
          <Text className="text-neutral-600 text-sm">
            If this background is rose-colored, the pipeline is correct.
          </Text>
        </View>
      );
    }
    ```

    IMPORTANT: `bg-rose-500` uses Tailwind's built-in rose color (not yet a custom token). This is intentional — we are testing that NativeWind v4 processes any className, not that our custom tokens work yet. Custom token smoke test happens in Plan 02.

    After writing all files, create the `src/components/` directory structure and the `src/` directory if they don't exist.
  </action>
  <verify>
    <automated>npx jest tests/smoke.test.tsx --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>All six config files exist. metro.config.js uses withStorybook(withNativeWind(...)) order. babel.config.js has jsxImportSource: "nativewind". SmokeTest component renders via className with no StyleSheet import. Running `npx expo export --platform ios --non-interactive` exits 0.</done>
</task>

</tasks>

<verification>
Overall plan checks:
1. `npx expo export --platform ios --non-interactive` exits 0
2. `npx jest tests/smoke.test.tsx` — fails with "Cannot find module" until SmokeTest exists (stub was created in Plan 00); after this plan it should find the module and pass
3. babel.config.js contains `jsxImportSource: "nativewind"` and `"nativewind/babel"`
4. metro.config.js pattern: `withStorybook(nativeWindConfig, ...)` — Storybook wraps NativeWind
5. global.css contains `@tailwind base; @tailwind components; @tailwind utilities;`
6. tailwind.config.ts has `presets: [require("nativewind/preset")]` — NOT in plugins array
7. nativewind-env.d.ts has `/// <reference types="nativewind/types" />`
</verification>

<success_criteria>
The Expo project builds without errors. A component can receive className="bg-rose-500" and render a rose-colored background on device — no StyleSheet required. The three-file NativeWind config (babel + metro + global.css) is wired together correctly and will support the full token system in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
